<div class="row">
  <div class="col-sm-12">
    <h1>Fogsync Client Settings</h1>
  </div>
  <div class="col-sm-12">
    <form role="form">
      <div class="form-group">
        <label for="user-email">Cloud Login Email</label>
        {{input id="user-email" class="form-control" value=model.Email 
          placeholder="you@example.com"}}
      </div>
      <div class="form-group">
        <label for="cloud-passwd">Cloud Login Password</label>
        {{input id="cloud-passwd" class="form-control" value=model.Passwd
          placeholder="8 to 32 characters"}}
      </div>
      <div class="form-group">
        <label for="cloud-server">Cloud Server</label>
        {{input id="cloud-server" class="form-control" value=model.Cloud
          placeholder="fogsync.com"}}
        <div class="form-desc">
          <p>For fogsync.com, this should be "fogsync.com".</p>
        </div>
      </div>
      <div class="form-group">
        <label for="master-key">Secret Master Key</label>
        {{input id="master-key" class="form-control" value=model.Master
          placeholder="32 hex digits"}}
        <div class="form-desc">
          <p>This key is used to secure all your data. Do not share it with
          any person or web site, including your cloud service.</p>
          <p>If this is your first device, use the randomly generated default
          key. Otherwise, type in your existing master key.</p>
        </div>
      </div>
      <div class="form-buttons">
        <button class="btn btn-primary" {{action save}}>Save</button>
        {{#if model.isDirty}} There are unsaved changes. {{/if}} 
      </div>
    </form>
  </div>
</div>
